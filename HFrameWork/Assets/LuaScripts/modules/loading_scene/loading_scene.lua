local base_scene = require('base_scene')

local loading_scene = gClass.declare("loading_scene",base_scene)

function loading_scene:on_loaded()

end

function loading_scene:update_prog_value(value)

end

return loading_scene